{
  "name": "escavello",
  "private": true,
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "npm run check && vite build && node scripts/check-site.mjs dist",
    "check": "node scripts/check-site.mjs",
    "preview": "vite preview",
    "test": "node --test scripts/*.test.mjs"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  },
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  }
}
